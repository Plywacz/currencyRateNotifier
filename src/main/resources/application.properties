spring.datasource.url = jdbc:mysql://localhost:3306/INSERT-YOUR-DB-NAME-HERE?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username = root
spring.datasource.password =

spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5InnoDBDialect

spring.jpa.hibernate.ddl-auto = create-drop

#quartz  config
spring.quartz.job-store-type=memory
spring.quartz.properties.org.quartz.threadPool.threadCount=5

#email address that is used by app to send notification
email.address=sample@mail.com
email.password=samplePasswd

#for example smtp.gmail.com
smtp.host=smtp.example.com
smtp.port=587

#Security settings
security.token_validity=10*60*60
security.signing_key="m_plywacz"
security.token_prefix="Bearer "
security.header_string="Authorization"
security.authorities_key="scopes